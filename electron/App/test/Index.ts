import { Index, IndexConstrcutor } from "..";
import { IIndex } from "../Abstraction/Index";

var assert = require("assert");
describe("#Index", function () {
  it("Add game works when get all games", async function () {});
});
