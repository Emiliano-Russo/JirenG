export interface Game {
  downloadLinks?: string[];
  crackUrl?: string;
  magnetUri?: string;
  title: string;
  imgUrl: string;
  totalSize: string;
  exeName?: string;
  tested?: boolean;
  version?: string;
}
